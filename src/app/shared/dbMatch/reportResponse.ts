import {Content} from './content';

export class ReportResponse {
      content:Content[];
      last:string;
      totalPages:string;
      totalElements:string;
      numberOfElements:string;
      size:number;
      first:string;
      number:string;
}